# 四则运算生成器 Qt

软件工程作业，四则运算生成器。

尚未完工。

# Usage

首先确保安装Qt相关组件和CMake。

```
$ git clone https://github.com/se-hwk-group/ArthemeticQt.git
$ cd ArthmeticQt
$ mkdir build && cd build
$ cmake .. && make -j8
$ ./Arthmetic

```
